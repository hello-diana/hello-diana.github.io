# Unverified / Owner-Confirmation Items

These could not be fully verified from official sources and should be confirmed by the owner.

1. **PuffEM & Paper-Based Tactile Interfaces — figures.** Both are behind ACM/IEEE paywalls with
   no open-access PDF found, so no figure thumbnail was added (no figure was fabricated). The
   citation/abstract metadata is verified (abstracts via Semantic Scholar, which mirrors the
   official ACM abstracts). If an open PDF becomes available, drop a `featured.png` into the
   respective `content/publication/<slug>/` folder.

2. **PuffEM equal contribution.** Marked as equal first-authorship (Wang* & Goel*) based on the
   September 2025 CV. Confirm against the published front matter if possible.

3. **Scito2M vs. SciEvo.** The arXiv record (v2) renamed the dataset from "Scito2M" to "SciEvo".
   The entry keeps the title "Scito2M …" (per the task brief and the CV) and notes the SciEvo
   name and the GitHub link `https://github.com/Ahren09/SciEvo`. The **Best Paper Award @ Good
   Data @ AAAI 2025 Workshop** is sourced from the CV (it is not stated in the arXiv comments).

4. **CompanionCast acceptance date.** arXiv comments confirm acceptance at the *ACM CHI 2026
   Workshop on Human-Agent Collaboration*. The exact acceptance date is unknown; the news post
   is dated to the arXiv v2 revision (2026-03-08) as an approximation. Adjust if you have the
   real date.

5. **CultivAgents co-author spelling.** Author "Moeiini Reilly" is taken verbatim from the arXiv
   metadata; please confirm the spelling.

6. **Georgia Tech thesis/project** "Determining Infectious Disease Positivity Rate Over
   Interaction Rate Through Analysis of Collocation Data" (seen on Google Scholar) was **not**
   added to the peer-reviewed publication list, since no official repository page could be
   verified to classify it as a thesis/other work. Provide a link if you want it listed.

7. **Orphaned talk page.** `content/event/mlgt2025/` still carries the older CompanionCast title
   ("Reimagining Sports Streaming with Multi-Agent AI Companions"). The Talks section/menu was
   removed, so it is no longer linked, but the page still exists if reached directly. Remove or
   update if desired.

## Recommendation: newer CV

The linked CV is dated **September 2025** (labeled `CV (Sep. 2025)`) and predates the 2026 work
(MASCOT, CultivAgents, UniSD, TextReg, and the CompanionCast acceptance). When an updated CV is
available, add it to `static/uploads/` (e.g. `cv-yiyang-diana-wang-<date>.pdf`) and update the
filename/label in `config/_default/menus.yaml`.
